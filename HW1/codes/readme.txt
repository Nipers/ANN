文件plot.py是用于根据记录的loss和accuracy数据画出图像，因为提交内容中不含两种数据，所以已不可运行。
关于记录的数据和原图如有需要可以提供。
为了便于记录数据，更改了solve_net.py中train和test函数的返回值，run_mlp函数的流程。
为多个类增加了__str__函数便于生成文件
目前的run_mlp由于后续调节参数实验的进行，只能输出固定超参数下单个网络每个epoch的准确率，如需要其他用途需自行更改。
